# self-stream-kit

**OBS StudioとMediaMTXを使って、Windows PCに自分専用の配信環境を構築するための自鯖配信キットです。**

[![Latest Release](https://img.shields.io/github/v/release/ginsabo/self-stream-kit?label=latest&style=flat-square)](https://github.com/ginsabo/self-stream-kit/releases/latest)
[![GitHub Pages](https://img.shields.io/badge/Web-site-67e8c2?style=flat-square)](https://ginsabo.github.io/self-stream-kit/)

[最新版をダウンロード](https://github.com/ginsabo/self-stream-kit/releases/latest) ・ [紹介ページを見る](https://ginsabo.github.io/self-stream-kit/) ・ [匿名で意見を送る](https://box.hat-work.com) ・ [GitHub Issues](https://github.com/ginsabo/self-stream-kit/issues)

## self-stream-kitとは

普段の配信操作には**OBS Studio**、映像を受け取って配信するサーバーには**MediaMTX**、視聴ページやコメントなどの機能には**Node.js**を利用します。

外部の配信サービスだけに頼らず、自分で管理できるストリーミング環境をWindows上に用意したい人向けのプロジェクトです。

```text
┌────────────────┐       映像を送信       ┌────────────────┐
│   OBS Studio   │ ─────────────────────▶ │    MediaMTX    │
│   配信・録画    │                        │  映像サーバー   │
└────────────────┘                        └───────┬────────┘
                                                │ HLS
                                                ▼
                                      ┌────────────────────┐
                                      │  self-stream-kit   │
                                      │  視聴・コメント管理 │
                                      └─────────┬──────────┘
                                                │
                                                ▼
                                      ┌────────────────────┐
                                      │       ブラウザ       │
                                      └────────────────────┘
```

## 特長

- **Windows向け** — Windows PCでの利用を想定しています。
- **OBS Studioを利用** — 使い慣れたOBSの画面から配信と録画を行えます。
- **MediaMTXを利用** — 軽量なストリーミングサーバーで映像を扱います。
- **自分で管理** — 配信環境を自分のPCとネットワーク上に用意できます。
- **コメント・リアクション** — コメント、そうだね、画像・動画・音声リアクション、落書きを利用できます。
- **アンケート・ピン止め** — コメント欄上部でアンケートとピン止めコメントを同時に表示できます。
- **アーカイブ対応** — 配信録画と、その配信中のコメントを関連付けて表示できます。
- **複数画質に対応** — 入力コーデックと利用環境に応じて、高画質、互換高画質、低画質を扱えます。
- **安全な更新とロールバック** — 利用者データを保持したまま更新し、問題があれば更新前へ戻せます。
- **Cloudflare Tunnelは任意** — LAN内だけで利用することも、設定してインターネットへ公開することもできます。

## はじめ方

1. [最新リリース](https://github.com/ginsabo/self-stream-kit/releases/latest)を開きます。
2. リリースページから最新版の配布用キットをダウンロードします。
3. 同梱されている導入ガイドを確認します。
4. Node.jsとMediaMTXを用意し、`0_初回セットアップ.bat`を実行します。
5. `1_配信をはじめる.bat`を実行します。
6. 表示された接続情報をOBS Studioへ設定して配信を開始します。

> [!IMPORTANT]
> インターネット経由で公開する場合は、認証、ファイアウォール、ルーター、Cloudflare Tunnelなどの設定を確認してください。
>
> 意図しない第三者からアクセスされないよう、公開範囲は利用者自身の責任で適切に管理してください。
>
> Discord Webhook URL、管理パスワード、秘密設定、Cloudflareの認証情報などを、ログ、スクリーンショット、Issueへ掲載しないでください。

## 2026-08-27 更新内容

視聴者からの不具合報告をもとにした修正が中心の更新です。全画面のままチャットが使えるようになり、視聴が苦しいときの自動復帰も強くなりました。リリース前に多角的な監査を行い、検出した問題は修正済みです。

### 全画面でもチャットが使えます

「全画面」ボタンで映像がディスプレイいっぱいになり、画面の右端にカーソルを寄せるとチャット欄が重なって出ます。そのままコメントもリアクションも送れ、カーソルが離れると消えます。タブレットは右端をタップ、キーボードはTabキーで開けます。動画プレイヤー右下の全画面ボタン（⛶）を押した場合も自動でこちらへ切り替わります。Escで元に戻ります。iPhoneとスマホの縦画面は仕組み上、全画面中のチャットに対応できません。

### リアクションのタブがスクロールに追従

画像・音声・動画のタブが、一覧をスクロールしても上端に貼り付いたままになります。下の方から別のタブへ移るのに、いちいち上まで戻る必要がなくなりました。視聴ページとチャット別窓の両方に効きます。

### 誤った「再生できませんでした」の表示を修正

一番軽い画質で回線が苦しいとき、普通に見えているのに「映像を再生できませんでした。利用可能な退避画質がありません」と表示されていました。今後は「回線が追いつかず映像が不安定です」という案内に変わり、再生が安定すれば自動で消えます。この表示が出ている間、視聴者数からその人が消えてしまう問題も同時に解消しました。

### 映像の遅れが伸びっぱなしになる問題に対処

狙い6秒に対して80秒以上遅れたまま戻らない状態を、キットが検知できていませんでした。再生位置とは独立した見張りを追加し、遅れが上限を大きく超えたまま戻らないときは配信の先端へ自動で寄せ直します。あわせて、手元の映像が完全に枯れた視聴者ほど軽い画質へ自動で落ちなかった穴も塞ぎました。作動した回数は管理画面の視聴品質メトリクスに `latency=` として出ます。

### 互換高画質のビットレート設定を分かりやすい場所へ

HEVCで配信する場合に関わる大事な設定が「詳細設定」の折り畳みに埋もれ、存在に気づけませんでした。折り畳みの外に出して「互換高画質のビットレート」に改名し、説明を添えました。さらに、OBSから届いている映像のビットレートを実測し、設定が大きすぎる間は管理画面に警告を出します。大きすぎても画質は上がらず、視聴者への送信量だけ増えて回線を圧迫します。軽画質のビットレートについても同じ警告が出ます。

### 管理画面の視聴状況の集計を修正

どの画質を誰が見ているかの集計が、すべて「unknown」に積まれていて切り分けに使えていませんでした。画質ごとの視聴状況が正しく記録されるようになり、遅れの自動復帰の回数も新たに確認できます。

### 品質確認

- 更新後テスト901件すべて成功
- 視聴ページ・別窓・管理画面・オーバーレイを実ブラウザで動作確認、配信者の実環境でも確認
- リリース前に多角的な監査（6系統のレビュー＋所見ごとの再検証）を実施し、検出した問題はすべて修正済み

## 2026-08-22 更新内容

リアクションの整理（カテゴリ・名前の変更）と音量・試聴の追加を中心に、動画登録の不具合も修正しました。リリース前に全ファイルの監査を行い、検出した問題を修正済みです。

### 素材ごとの音量と、送る前の試聴

音声・動画リアクションの音量を素材ごとに50〜200%で設定できます。管理画面の編集欄で設定し、全体の「音声／動画リアクション音量」に掛け合わせて鳴ります。視聴者は視聴ページ・別窓の「▶ 試聴」で手元だけで確かめ（配信には出ません）、「送る音量」でその回だけ50〜200%を選べます。100%超は配信画面側で増幅します。OBSのブラウザソースで動画の音量が反映されない事象にも対処しました。

### カテゴリで折り畳み

登録時にカテゴリ（任意）を付けると、視聴ページ・別窓の一覧がカテゴリごとの折り畳みになります。未分類は「その他」にまとめて最後に置き、開閉は端末ごとに記憶します。カテゴリを付けていない配信は今までどおりの平らな一覧です。

### ボタン名は必須、後から直せる

ボタン名が空欄だとファイル名がそのまま登録され、後から直す手段が無かった問題を解消しました。空欄では登録できず、管理画面の「登録済みリアクション素材」から名前・カテゴリ・音量をいつでも変更できます。変更は見ている全員の一覧にその場で反映されます。

### 管理画面の素材一覧を整理

並び替え（追加が古い順・新しい順・名前順）、カテゴリごとの束ね、各素材のプレビュー（画像・音声・動画）を追加しました。

### 不具合の修正

- 動画の登録が手元で5MBまでに制限され、案内どおり20MBまでの動画が登録できなかった問題を修正しました
- 音声リアクションが鳴り終わった後の音の経路を毎回片付け、長時間配信での蓄積を防ぐようにしました
- 音量の内部処理（50〜200%の丸め）を一本化しました

### 品質確認

- 更新後テスト865件すべて成功
- 視聴ページ・別窓・管理画面・オーバーレイを実ブラウザで動作確認、配信者の実環境（実OBS・実MediaMTX）でも確認
- リリース前に全ファイル監査を実施し、検出した問題を修正済み

## 2026-08-14 更新内容

視聴中に読み込みの「ぐるぐる」が出る問題への対策です。チャット欄が表示されているだけで映像の再生を圧迫していた原因を複数特定し、まとめて解消しました。

- コメントの金色・虹色装飾の流れるアニメーションを廃止し、静止表示にしました（装飾行が見えている間じゅう画面の描き直しを強いていたため）
- リアクション一覧のサムネイルを静止画化し、GIF等が動き続けて映像と取り合うのを防ぎました
- WebSocket切断中の履歴取得で最大300行を作り直していた処理を、行の使い回しへ変更しました
- 落書きの描画ループを、線が無い間は待機するようにしました
- アンケートの残り時間表示を、文字だけの書き換えにしました
- 視聴ページとチャット別窓で重複していた処理を共通部品（chat-shared.js）へ一本化しました

## 2026-08-11 更新内容

視聴画質の自動切替（ABR）の追加を中心に、再生の安定性・エコー対策・導入の自動化を進めました。外部AI監査を3巡実施し、指摘された問題をすべて修正済みです（最終判定: Critical 0 / High 0 / Medium 0）。

### 自動画質（ABR）を追加しました

H264配信で画質「自動」を選ぶと、視聴者ごとの回線の太さに合わせて高画質と軽画質を自動で行き来します。回線が細くなると自動で軽く、回復すると自動で高画質へ戻ります。視聴者の操作は不要です。対象は配信側で「低画質の生成」をONにしたH264配信で、HEVC配信は従来どおりの自動選択（起動時に最適を選び、苦しくなったら自動で一段落とす）です。

### MediaMTXの自動取得batを追加しました

「5_MediaMTXを用意する.bat」で、動作確認済みの版のMediaMTXをwingetから自動導入できるようになりました。既存の設定（mediamtx.yml）は上書きせず、壊れたexeや設定だけが消えた環境も自動で修復します。

### 再生の立て直しを強化しました

- 音は流れるのに映像だけ凍る故障を自動検出し、3段階で自動復旧します
- 読み込みが追いつかない状態が続く視聴者を、自動で一段軽い画質へ切り替えます
- 映像の保持時間を約7秒から約20秒へ広げ、「読み込みのぐるぐるが止まらない」事故を減らしました

### 読み込みと画質切替を高速化しました

- 読み込みの再試行間隔を短縮し、「再接続を押した方が早い」状態を解消しました
- 軽画質への切替は変換の準備完了を待ってから行う方式に変更し、切替に失敗して元の画質へ戻される問題をなくしました
- 配信開始直後に画質が勝手に切り替わって起動を妨げる問題を修正しました

### エコー対策を追加しました

- ポップアップ視聴時は元ページの再生を自動停止し、同じ配信の二重再生によるエコーを解消しました
- OBSが音声を複数トラックで送っている場合（「高画質だけエコーする」原因）は、管理画面の健全性チェックへ警告を出します
- チャットの受付を一時停止できる機能を追加しました

### 画面と説明書を改善しました

- ウィンドウ内全画面でも画質メニュー・落書き・スクショを使えるようにし、切替後にツールバーが消えなくなる問題を修正しました
- 更新手順の説明を「更新する.bat」へ統一しました
- publicフォルダーに置いたファイルは視聴者からも取得できる旨と、独自に追加・改造したファイルの更新時の扱いを説明書へ明記しました

### 品質確認

- 更新後テスト832件すべて成功
- 外部AI監査3巡で指摘された問題（High 1件・Medium 10件）をすべて修正
- 隔離環境に加え、実OBS・実MediaMTX・実スマートフォンでの実環境検証を実施

## 2026-08-07 更新内容

2026-08-05版で確認された、視聴画面の表示崩れと更新処理の不具合を修正しました。

### ピン止めコメント表示時に入力欄が隠れる問題を修正しました

ピン止めされたコメントの「全文」を押すと本文が上限なく伸び、コメント入力欄が画面の外へ押し出されて書き込めなくなることがありました。アンケートと同時に表示されている場合は、「全文／折り畳む」「元へ」の操作ボタンごと見えなくなるケースもありました。

- チャット上部の固定表示エリアに高さの上限と内部スクロールを設けました。
- 視聴ページと別窓チャットで同じ表示制御を使うようにしました。
- スマートフォンの固定入力欄と、幅の狭い別窓チャットでも、名前・コメント・送信操作が表示範囲内に収まるようにしました。

### コメント文字数の上限が反映されていなかった問題を修正しました

管理画面でコメントの最大文字数を変更しても視聴画面には反映されず、上限を超えた分は警告なく切り捨てられていました。

- 設定した上限を視聴ページと別窓チャットの入力欄へ反映するようにしました。
- 上限を超えたコメントは、無言で切り捨てず、理由を表示して送信を拒否するようにしました（HTTP・WebSocketの両方）。

### 更新が途中で止まる不具合を複数修正しました

環境によって、更新処理が途中で中止され、自動的に更新前の状態へ戻ってしまう問題がありました。

- OBSプロファイルのフォルダーが無い環境で更新が失敗する問題を修正しました。既存の配信環境にあるOBSプロファイルは、削除・一部削除・独自の内容に変更していても更新の妨げになりません（新規ダウンロード時の同梱プロファイルは、これまでどおり全件確認します）。
- `scripts`フォルダーに利用者が自分で追加したファイルがあると更新できない問題を修正しました。
- 導入先に利用者が追加したJS・CSS・独自テストがあると更新できない問題を修正しました。
- 特定の文字コードで生成された管理ファイルにより、配布直後だけ更新が失敗する問題を修正しました。

### 更新の実行結果が文字化けする問題を修正しました

`APPLY_UPDATE.ps1`・`ROLLBACK_UPDATE.ps1`をPowerShellから直接実行した場合に、日本語の実行結果が文字化けして読めないことがありました。`更新する.bat`・`もとに戻す.bat`からの実行と同じように、文字コードを初期化してから処理するよう修正しました。

### OBSプロファイルの取り扱いを明確にしました

更新手順書に「OBSプロファイルも自動で入れ替わる」と誤って書かれていました。実際には自動コピーされません。更新用ZIPに同梱しているプロファイルは、OBSへ手動で取り込んでください。

### 配布内容の確認を強化しました

- 視聴ページ・管理画面・別窓チャットなど、公開している全画面が同じ版の部品を読み込んでいるかを確認するテストを追加しました。一部の画面だけ古い部品が残る事態を防ぎます。
- 配布内容の一覧表を、配布用・更新用・更新キットの3種類すべてのZIPへ対応させ、相互に照合するようにしました。
- 配布後のテストが、検証用の控えではなく、実際にインストールされた起動bat・説明書・OBSプロファイルそのものを確認するようにしました。
- 前回リリースと今回リリースの配布内容を自動で比較し、廃止したファイルが退役リストへ正しく分類されているかを確認するようにしました。以前は手作業でリストを維持しており、追記漏れが更新失敗の原因になっていました。

### 内部整理

- 使われていない管理画面用のCSSを削除しました。
- アーカイブ画面で発生していたID重複を解消しました。
- 視聴ページと別窓チャットで重複していた入力欄の処理を、共通の部品へまとめました。
- CSSのインデントを統一しました。

### 自動テストと更新互換性

同梱自動テストを**787件**へ増やしました（テストファイル数 116→119）。

確認結果：

- 2026-08-05版がインストールされ、OBSプロファイルが存在しない環境への更新：成功
- 2026-08-05版がインストールされ、利用者が追加したファイルやOBSプロファイルの一部変更がある環境への更新：成功
- 更新後テスト：787件すべて成功
- 管理コード・利用者データの差分：0
- ロールバック：成功
- ロールバック後の管理コード・利用者データ・MediaMTX設定の差分：0
- 2026-07-30版・2026-08-05版からの退役ファイル判定：既知の対象を正しく検出

これらは隔離したローカル環境での確認結果です。実OBS、実MediaMTX配信、実Cloudflare Tunnel、実スマートフォン、実Discord Webhookは利用者環境での確認が必要です。

## 2026-08-05 更新内容

2026-08-04版で確認された起動・更新不具合と、安全性・操作性の問題を修正しました。

### ランチャーがMediaMTX起動前に停止する問題を修正しました

2026-08-04版では、ランチャーが次のPowerShell関数を見つけられず、MediaMTXを起動する前に停止する場合がありました。

```text
Test-ExplicitLocalRtspRequested
```

更新処理とランチャーが同じ共有ライブラリから判定処理を読み込むよう修正しています。

`mediamtx.yml`が壊れていない場合に、ランチャー側の実装漏れで設定エラーと表示される問題も解消しました。

### 初回セットアップと更新がアクセス権エラーで停止する問題を修正しました

Windowsが同じアクセス権を継承情報だけ正規化した場合でも、次のエラーとして扱われることがありました。

```text
MediaMTX config access control changed
```

実際のアクセス権変更と、Windowsによる安全な正規化を区別するよう修正しました。

MediaMTX設定の原子的置換や復旧に失敗した場合も、元の設定内容、ファイル属性、アクセス権を一組として復元・検証します。

所有者や監査ACLを勝手に変更することはありません。

### ピン止め機能の場所を分かりやすくしました

管理画面の項目名を、次のように変更しました。

```text
最近のコメント・通報・ピン止め
```

この項目は管理画面の「視聴者」タブにあります。

- 現在の配信セッションに投稿されたコメントだけをピン止めできます。
- 配信停止中はピン止めボタンを表示しません。
- ピン止めできない場合は理由を表示します。
- 削除・通報されたコメントは固定できません。
- 現在のピン止め状態を常時確認できます。

### 管理画面の項目を折り畳めるようにしました

管理画面の各カードへ、共通の折り畳み操作を追加しました。

```text
▼ 展開中
▶ 折り畳み中
```

- 項目名をクリックして開閉できます。
- 折り畳んでも入力内容や設定値は維持されます。
- 項目が増えたタブでも、必要な場所へ移動しやすくなりました。
- キーボード操作と`aria-expanded`にも対応しています。

### アーカイブコメントAPIの公開範囲を修正しました

公開アーカイブが存在しない配信セッションのコメントを、セッションIDだけで取得できる問題を修正しました。

- 公開されているアーカイブと関連付いているコメントだけを返します。
- アーカイブを削除すると、対応するコメントAPIも公開されなくなります。
- 過去ログ自体は削除しません。
- 管理者向けの保存データはそのまま維持します。

### 管理パスワード消失時の動作を安全にしました

インストール済み環境で、秘密設定と旧パスワードファイルが両方失われた場合に、既知の初期パスワードへ戻る動作を廃止しました。

パスワード情報が破損・消失している場合は安全停止し、復旧方法を表示します。

既存の管理パスワードが正常に保存されている環境では変更されません。

### 配信セッションの集計を修正しました

完了済みの配信セッションへ後から通報を記録した場合に、リアクション件数だけが再構築後の集計から抜ける問題を修正しました。

コメント、削除、通報、そうだね、リアクションの集計処理を共通化し、同じデータから一貫した結果を生成します。

### コメジェネ連携を非同期化しました

コメント投稿時に行っていたコメジェネXMLの同期読み書きを、非同期の直列キューへ変更しました。

- 遅いディスクやネットワーク上の保存先でも、Node.js全体を同期停止しにくくしました。
- コメント、HTTP、WebSocket、視聴処理への影響を抑えます。
- 待ち行列は最大256件に制限し、無制限にメモリを消費しないようにしています。
- 書き込み失敗時もプロセス全体を停止しません。

### 配布ZIP生成処理を整理しました

配布物の生成経路を、正式なパッケージ生成処理へ一本化しました。

- 安全な更新機構を通らない古い更新スクリプトを削除しました。
- 別の規則でZIPを生成していた古いreleaseスクリプトを削除しました。
- 新規配布、更新用、検証用で同じコピー処理を利用します。
- `.log`、`.zip`、`.bak`、一時ファイルを共通の規則で除外します。
- 利用者の設定、秘密情報、ログ、素材、アーカイブを配布ZIPへ含めません。

### 自動テストと更新互換性

同梱自動テストを**770件**へ増やしました。

確認結果：

- 2026-07-30版の更新前テスト：743件すべて成功
- 2026-08-05版への更新：成功
- 更新後テスト：770件すべて成功
- 利用者データ差分：0
- 更新用BATからの実行：成功
- 更新バックアップの自動生成：成功
- ロールバック：成功
- ロールバック後の管理コード差分：0
- ロールバック後の利用者データ差分：0
- ロールバック後テスト：743件すべて成功

これらは隔離したローカル環境での確認結果です。実OBS、実MediaMTX配信、実Cloudflare Tunnel、実スマートフォン、実Discord Webhookは利用者環境での確認が必要です。

## 2026-08-04 追加機能

### コメントを配信ごとに区切るようにしました

前回の配信コメントが次の配信にも残り続けないよう、コメントを「1回の配信」単位で管理します。

- 新しい配信を始めると、通常のコメント欄は空の状態から始まります。
- 過去のコメントログは削除せず、そのまま保存します。
- 30秒以内の瞬断やNode.jsの再起動では、同じ配信として復元します。
- 30秒を超えて停止したあと再開すると、新しい配信として扱います。
- 配信停止中に書かれたコメントは、次の配信へ持ち越しません。

### アーカイブに、その配信のコメントを表示できるようになりました

録画の時刻から対応する配信を特定できる場合、その配信中のコメントをアーカイブ画面へ表示します。

自動で判定できない録画は、録画ファイルの隣にあるJSONへ配信セッションIDを指定して手動で関連付けられます。

古い録画や古いコメントは削除せず、関連付けなしの状態で保持します。

### Discord通知で`@everyone`と`@here`を使えるようになりました

管理画面に、Discord通知の全員メンションを許可する設定を追加しました。

- 初期設定はOFFです。
- ONにしても、通知文に実際に`@everyone`または`@here`が書かれている場合だけ反応します。
- ユーザーやロールへの任意メンションは許可しません。
- Discord側で全員メンションを使用できる権限も必要です。

### MediaMTXを差し替えたあとの復旧を改善しました

MediaMTXのバージョンを差し替えたあと、HEVCや互換高画質などの選択肢が消え、元へ戻しても復旧しない場合がありました。

配信開始時に`mediamtx.yml`を再確認し、不足している安全な設定だけをバックアップ付きで修復します。

- 推奨バージョンは引き続き**v1.19.1**です。
- それ以外のバージョンでは警告を表示します。
- バージョン番号だけを理由に画質を非表示にしません。
- Control APIが復旧すると、更新BATを再実行しなくても画質状態を再判定します。
- 修復前の設定は`mediamtx\self-stream-config-backups`へ保存します。
- 未確認バージョンを正式対応として扱うものではありません。

### コメントのピン止め機能を追加しました

管理者が、現在の配信中のコメントを1件だけチャット上部へ固定できます。

- ピン止めと解除は管理者だけが行えます。
- 削除・通報されたコメントは自動で解除されます。
- 配信終了時に解除され、次の配信へ持ち越しません。
- Node.jsを再起動しても、同じ配信中であればピン止めを復元します。
- 投稿者名、本文、投稿時刻を表示します。
- 元のコメントは通常コメント欄に残ります。

### アンケートとピン止めが重ならないようにしました

チャット上部を共通の固定表示エリアに変更しました。

表示順：

1. アンケート
2. ピン止めコメント
3. 通常コメント

PC、スマートフォン、別窓表示でも縦に並び、互いに重なりません。

### そのほかの修正

- Node.js再起動時のコメントログと集計の復元処理を改善しました。
- 一部だけ書き込まれたログは、欠けた処理だけを再開するよう修正しました。
- 配布元の診断BATを、Windowsで安定して読めるCRLF・UTF-8 BOMなしへ修正しました。
- 不要になったAPIや重複していた処理を整理しました。

## 2026-07-30版・2026-08-04版・2026-08-05版からの更新方法

2026-07-30版、2026-08-04版、または2026-08-05版を利用している場合は、更新用キットを使用できます。

1. 配信を停止します（`2_配信をとめる.bat`）。
2. 「自鯖配信キット_更新」フォルダーを、現在使用している配信キットのフォルダー内へ置きます。
3. `更新する.bat`を実行します。
4. 数分かかるため、処理中は窓を閉じないでください。
5. 「Update completed」と表示されたら完了です。
6. `1_配信をはじめる.bat`から起動します。

更新時に保持されるもの：

- 管理パスワード
- 配信設定
- Cloudflare設定
- Discord設定
- コメントログ
- セッションログ
- リアクション素材
- 待機画面
- アーカイブ
- 利用者独自画像
- `mediamtx.exe`
- 既存の`mediamtx.yml`
- OBSプロファイル（利用者が追加・変更・削除している場合、その状態のまま維持されます）

更新前のファイルは`self_stream_update_backups`へ保存されます。

更新がうまくいかない場合は、`もとに戻す.bat`を実行すると、最新の更新バックアップを自動で選択して更新前の状態へ戻します。

> [!IMPORTANT]
> 更新中にエラーが表示された場合は、同じ更新を何度も重ねて実行する前に、表示されたエラーとログを保存してください。
>
> `mediamtx.yml`、設定、秘密ファイル、アーカイブ、コメントログを手動で削除しないでください。

## 必要なもの

- Windows PC
- Node.js 20以降
- OBS Studio
- MediaMTX
- ネットワーク接続
- self-stream-kitの最新リリース

推奨MediaMTXバージョンは**v1.19.1**です。

低画質やHEVCからH.264への互換変換を利用する場合は、FFmpegも必要です。

対応バージョンやリリース固有の注意事項は、[最新リリースの説明](https://github.com/ginsabo/self-stream-kit/releases/latest)を確認してください。

## リンク

| 内容 | URL |
| --- | --- |
| 紹介ページ | <https://ginsabo.github.io/self-stream-kit/> |
| 最新版ダウンロード | <https://github.com/ginsabo/self-stream-kit/releases/latest> |
| リリース一覧 | <https://github.com/ginsabo/self-stream-kit/releases> |
| 匿名の感想・気軽な要望 | <https://box.hat-work.com> |
| 詳細な不具合報告・開発相談 | <https://github.com/ginsabo/self-stream-kit/issues> |

## このリポジトリについて

リポジトリ直下の[`index.html`](./index.html)は、GitHub Pagesで公開する紹介ページです。

外部ライブラリやビルド処理を必要とせず、HTMLファイル単体で動作します。

```text
self-stream-kit/
├── index.html   # GitHub Pages用の紹介ページ
└── README.md    # このドキュメント
```

## フィードバック

目的に合わせて、2つの窓口を利用できます。

### 匿名ご意見ボックス

「少し気になった」「こんな機能がほしい」など、気軽に伝えたい場合は[匿名ご意見ボックス](https://box.hat-work.com)をご利用ください。

GitHubアカウントは必要ありません。

### GitHub Issues

再現手順がある不具合、ログを使った調査が必要な問題、具体的な改善提案は[GitHub Issues](https://github.com/ginsabo/self-stream-kit/issues)をご利用ください。

不具合を報告する場合は、可能な範囲で次の情報を記載してください。

- 利用しているself-stream-kitのバージョン
- Windows、OBS Studio、MediaMTX、ブラウザのバージョン
- 問題が起きるまでの操作手順
- 表示されたエラー
- 再現するかどうか
- Cloudflare Tunnelを使用しているか
- H.264またはHEVCのどちらを使用しているか
- 高画質、互換高画質、低画質のどれで発生したか

Webhook URL、管理パスワード、Cookie、秘密設定、生IP、Cloudflareの認証情報などは掲載しないでください。
